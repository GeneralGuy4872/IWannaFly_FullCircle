bool argv_and (unsigned argc,bool argv[]);
bool argv_or (unsigned argc,bool argv[]);
bool argv_nand (unsigned argc,bool argv[]);
bool argv_nor (unsigned argc,bool argv[]);
bool argv_onehot (unsigned argc,bool argv[]);
bool argv_evenparity (unsigned argc,bool argv[]);
bool argv_oddparity (unsigned argc,bool argv[]);
bool argv_iff (unsigned argc,bool argv[]);